import * as Tooltip from '@radix-ui/react-tooltip';

const TooltipComponent = () => {
  return (
    <Tooltip.Provider>
      {/* Tooltip content */}
    </Tooltip.Provider>
  );
};

export default TooltipComponent;